# workio / quality

**High-Value Incremental Improvement for Workio Frontend and Backend**
### Implementation Plan

**Task:** Implement HF CDN Bypass for Training Pipeline and Surrogate-1 Training Pipeline

**Time Estimate:** 1 hour 30 minutes

**Implementation Steps:**

1. **HF CDN Bypass:**
	* Create a new file `cdn_bypass.py` in the `workio/server` directory with the following code:
```python
import requests
import json

def get_file_list(repo, path):
    url = f"https://huggingface.co/datasets/{repo}/resolve/main/{path}"
    response = requests.get(url)
    if response.status_code == 200:
        return json.loads(response.content)
    else:
        return []
```
2. **Embed file list in training script:**
	* Open the `train.py` file in the `workio/server` directory and add the following code:
```python
import cdn_bypass

# ...

file_list = cdn_bypass.get_file_list("your-repo-name", "your-date-folder")
```
3. **Update training pipeline:**
	* Open the `training_pipeline.py` file in the `workio/server` directory and update the `load_data` function to use the CDN bypass:
```python
import cdn_bypass

# ...

def load_data():
    file_list = cdn_bypass.get_file_list("your-repo-name", "your-date-folder")
    # ...
```
4. **Test the implementation:**
	* Run the `train.py` script and verify that the training pipeline uses the CDN bypass.

**Commit Message:**
```
Implement HF CDN Bypass for Training Pipeline and Surrogate-1 Training Pipeline
```
**API Documentation:**
```markdown
## HF CDN Bypass

The HF CDN Bypass allows for downloading public dataset files from the Hugging Face CDN without authentication.

### API Endpoints

* `GET /cdn_bypass`: Returns a list of file paths for a given repository and date folder.

### Request Parameters

* `repo`: The name of the repository.
* `path`: The path to the date folder.

### Response

* A list of file paths.
```
