# airship / backend

**Highest-Value Incremental Improvement**
=====================================

**Task:** Implement HF CDN Bypass for Rate Limitation

**Implementation Plan:**
------------------------

### Step 1: Update `arkship/surrogate/ingestion.py`

```python
import requests

def download_file_from_cdn(repo, path):
    url = f"https://huggingface.co/datasets/{repo}/resolve/main/{path}"
    response = requests.get(url)
    return response.content
```

### Step 2: Update `arkship/surrogate/training.py`

```python
import os
import json

def load_dataset_from_cdn(repo, path):
    file_list = load_file_list_from_cdn(repo)
    for file in file_list:
        download_file_from_cdn(repo, file)
        # Load and process file as usual
```

### Step 3: Update `arkship/surrogate/utils.py`

```python
import json

def load_file_list_from_cdn(repo):
    url = f"https://huggingface.co/datasets/{repo}/resolve/main/list.json"
    response = requests.get(url)
    file_list = json.loads(response.content)
    return file_list
```

**Explanation:**

This implementation bypasses the HF API rate limit by downloading files directly from the CDN. We first update the `ingestion.py` file to include a function that downloads files from the CDN. Then, we update the `training.py` file to use this function to load the dataset from the CDN. Finally, we update the `utils.py` file to include a function that loads the file list from the CDN.

**Benefits:**

* Bypasses HF API rate limit
* Reduces API calls and improves performance
* Allows for faster training and ingestion of datasets

**Time Estimate:** 1 hour

**Tags:** #huggingface #cdn #rate-limit-bypass #training
