# Costinel / ops

**Highest-Value Incremental Improvement:**
**Pattern:** HF CDN Bypass
**Fix:** Public dataset files at `https://huggingface.co/datasets/{repo}/resolve/main/{path}` can be downloaded with NO Authorization header — bypasses /api/ auth-check rate limit entirely.

**Implementation Plan:**

1. **Verify the Fix:**
   - Run a test to confirm that downloading files from the CDN bypasses the rate limit.
   - Use a tool like `curl` or a programming language like Python to download a file from the CDN and verify that the rate limit is not exceeded.

2. **Update the Training Script:**
   - Modify the training script to download files from the CDN instead of using the HF API.
   - Use the `requests` library in Python to send a GET request to the CDN URL and download the file.

3. **Embed the File List in the Training Script:**
   - Use the `list_repo_tree` API to get a list of files in a specific folder.
   - Save the list to a JSON file.
   - Embed the JSON file in the training script.

**Code Snippet:**
```python
import requests

def download_file_from_cdn(repo, path):
    url = f"https://huggingface.co/datasets/{repo}/resolve/main/{path}"
    response = requests.get(url)
    if response.status_code == 200:
        return response.content
    else:
        return None

# Example usage:
repo = "your-repo-name"
path = "your-file-path"
file_content = download_file_from_cdn(repo, path)
if file_content:
    with open("file.txt", "wb") as f:
        f.write(file_content)
```
**Estimated Time to Complete:** 1 hour

**Tags:** #huggingface #cdn #rate-limit-bypass #training
