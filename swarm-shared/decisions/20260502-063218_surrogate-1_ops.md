# surrogate-1 / ops

**Surrogate-1 Ops Improvement**

### **Diagnosis**
* The project lacks a robust implementation for handling Hugging Face API rate limits, which can block dataset training.
* The existing implementation may not be reusing existing Lightning Studio instances efficiently.
* The existing implementation may not be properly handling Hugging Face API rate limits, which can lead to 429 errors.
* The existing implementation may not be utilizing the Hugging Face CDN to bypass rate limits.
* The existing implementation may not be properly handling the Hugging Face commit cap, which can block ingestion.

### **Proposed change**
* Update the `train.py` file to utilize the Hugging Face CDN to bypass rate limits.
* Update the `opus-pr-reviewer.sh` script to properly handle Hugging Face API rate limits.

### **Implementation**
1. Update the `train.py` file to use the `list_repo_tree` API method to fetch file paths from the Hugging Face CDN.
```python
import requests

# ...

repo = "your-repo-name"
date_folder = "your-date-folder"

# Fetch file paths from Hugging Face CDN
response = requests.get(f"https://huggingface.co/datasets/{repo}/resolve/main/{date_folder}")
file_paths = response.json()["files"]

# ...
```
2. Update the `opus-pr-reviewer.sh` script to properly handle Hugging Face API rate limits.
```bash
#!/bin/bash

# ...

# Check if the rate limit has been exceeded
if [ $? -eq 429 ]; then
  # Wait for 360 seconds before retrying
  sleep 360
  # Retry the API call
  response=$(curl -s -f "https://huggingface.co/api/datasets/${dataset}/files")
fi

# ...
```
### **Verification**
1. Run the `train.py` file and verify that it can fetch file paths from the Hugging Face CDN without hitting rate limits.
2. Run the `opus-pr-reviewer.sh` script and verify that it can properly handle Hugging Face API rate limits.
3. Verify that the Hugging Face commit cap is not exceeded during ingestion.
4. Verify that the Hugging Face CDN is being utilized to bypass rate limits.
