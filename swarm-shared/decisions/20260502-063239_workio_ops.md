# workio / ops

**High-Value Incremental Improvement for Workio Frontend and Backend**
### Implementation Plan

**Task:** Implement HF CDN Bypass for Training Pipeline and Surrogate-1 Training Pipeline

**Time Estimate:** 1 hour 30 minutes

**Implementation Steps:**

1. **HF CDN Bypass:**
	* Create a new script `cdn_bypass.py` in `workio/server` directory to handle HF CDN bypass.
	* In `cdn_bypass.py`, use `requests` library to download public dataset files from HF CDN without authentication.
	* Use `list_repo_tree` API to get file paths and save them to a JSON file.
	* Embed the JSON file in the training script `train.py` to load file paths during training.
2. **Surrogate-1 Training Pipeline:**
	* Update `train.py` to use the `cdn_bypass.py` script to download files from HF CDN.
	* Remove the `load_dataset` function and use `hf_hub_download` to download files individually.
	* Project the downloaded files to `{prompt, response}` format at parse time.

**Code Snippets:**

**cdn_bypass.py**
```python
import requests
import json

def get_file_paths(repo, path):
    url = f"https://huggingface.co/datasets/{repo}/resolve/main/{path}"
    response = requests.get(url)
    file_paths = response.json()["files"]
    return file_paths

def save_file_paths(file_paths, filename):
    with open(filename, "w") as f:
        json.dump(file_paths, f)

def main():
    repo = "your-repo-name"
    path = "your-date-folder"
    file_paths = get_file_paths(repo, path)
    save_file_paths(file_paths, "file_paths.json")

if __name__ == "__main__":
    main()
```

**train.py**
```python
import json
import torch
from transformers import AutoModelForSequenceClassification

def load_file_paths(filename):
    with open(filename, "r") as f:
        file_paths = json.load(f)
    return file_paths

def main():
    file_paths = load_file_paths("file_paths.json")
    # Use file_paths to download files from HF CDN
    # Project the downloaded files to {prompt, response} format at parse time
    model = AutoModelForSequenceClassification.from_pretrained("your-model-name")
    # Train the model
    pass

if __name__ == "__main__":
    main()
```

**Commit Message:**
`Implement HF CDN Bypass for Training Pipeline and Surrogate-1 Training Pipeline`
