# surrogate-1 / ops

### Diagnosis
* The project lacks a robust implementation for handling Hugging Face API rate limits, which can block dataset training.
* The existing implementation may not be reusing existing Lightning Studio instances efficiently, leading to wasted resources.
* The project does not have a mechanism to bypass the Hugging Face API rate limit entirely, which can be achieved by downloading public dataset files from the CDN.

### Proposed change
The proposed change is to implement a mechanism to bypass the Hugging Face API rate limit by downloading public dataset files from the CDN. This can be achieved by modifying the `train.py` script to download the dataset files from the CDN instead of using the Hugging Face API.

### Implementation
To implement this change, we need to modify the `train.py` script to download the dataset files from the CDN. We can use the `requests` library to download the files. Here is an example of how the code can be modified:
```python
import requests

# Define the dataset repository and file path
dataset_repo = "username/dataset"
file_path = "path/to/file"

# Download the file from the CDN
url = f"https://huggingface.co/datasets/{dataset_repo}/resolve/main/{file_path}"
response = requests.get(url)

# Save the file to disk
with open(file_path, "wb") as f:
    f.write(response.content)
```
We also need to modify the `list_repo_tree` function to download the list of files from the CDN instead of using the Hugging Face API. We can use the `requests` library to download the list of files. Here is an example of how the code can be modified:
```python
import requests

# Define the dataset repository and path
dataset_repo = "username/dataset"
path = "path/to/folder"

# Download the list of files from the CDN
url = f"https://huggingface.co/datasets/{dataset_repo}/resolve/main/{path}"
response = requests.get(url)

# Parse the list of files from the response
files = response.json()

# Save the list of files to disk
with open("files.json", "w") as f:
    json.dump(files, f)
```
We can then modify the `train.py` script to use the list of files downloaded from the CDN instead of using the Hugging Face API.

### Verification
To verify that the change works, we can run the `train.py` script and check that the dataset files are downloaded from the CDN instead of using the Hugging Face API. We can also check the logs to ensure that the API rate limit is not exceeded. Additionally, we can verify that the list of files is downloaded correctly from the CDN and used by the `train.py` script.
